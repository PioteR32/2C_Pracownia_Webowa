let  samogloski= [12,213,321];
let naj = 0;
    for(let i = 0; i < samogloski.length;i++)
    {      
        if( samogloski[i] > 10)
        {
            naj++;
        }
    }
    document.write(naj);